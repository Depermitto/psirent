package constants

const MAX_ADDR_NUM = 8        // Maximum number of clients that we can download from / can download from us
const FILE_CHUNK = 512 * 1024 // Minimum fragment size of a file in bytes
const PEER_PREFIX = "PEER>"   // Prefix for messages from the peer
const HOST_PREFIX = "HOST>"   // Prefix for messages from the host
